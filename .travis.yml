sudo: required
dist: trusty
services:
  - docker
before_install:
  - docker build docker/travis_test -t os-autoinst/travis_test:latest
script:
  - docker run --cap-add SYS_ADMIN -v $TRAVIS_BUILD_DIR:/opt/repo os-autoinst/travis_test:latest

