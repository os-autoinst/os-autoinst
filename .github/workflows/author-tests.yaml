---
name: Author tests

on: [push, pull_request]

jobs:
  author-tests:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    name: Author tests
    container:
      image: registry.opensuse.org/devel/openqa/containers/os-autoinst_dev
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # v6
      - name: make test-local
        run: |
          git config --global --add safe.directory '*'
          make
          # see .github/workflows/commit-message-checker.yml
          # for the git commit message check
          ctest --test-dir build --output-on-failure -R "test-local-.*" -E "test-local-git-commit-message"
