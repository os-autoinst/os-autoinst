on:
  push:
    branches:
      - '*'
    tags-ignore:
      - '*'
  pull_request:
    branches:
      - '*'

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - run: echo HELLO

#    - name: Cache image
#      uses: actions/cache@v1
#      env:
#        cache-name: cache-image
#      with:
#        path: ~/cache
#        key: cache-${{ hashFiles('Dockerfile') }}
#
#    - run: ls -lha ~/cache || true
#
#    - run: |
#        if [[ -f ~/cache/test.image ]]; then
#            echo "Image exists"
#            docker load -i ~/cache/test.image
#        else
#            docker build -t hello .
#            mkdir -p ~/cache
#            docker save --output ~/cache/test.image hello
#        fi
#        ls -lha ~/cache
#        docker images | grep hello || true
#
#    - run: |
#        docker run -i --rm hello
